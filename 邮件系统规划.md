# 邮件系统（Mailbox）实现规划

> 适用项目：`YouzaiWorldCore`（MC 26.2 / Fabric / Java 25 / Mixin 0.8.7）
> 关联入口：主菜单（`Shift+F`）底部按钮行第 2 格「邮件」占位符
> 当前状态：占位弹窗（`MainMenuElements` → `showNotImplementedDialog`）
> 文档版本：v5.4（最终评审修订：mail 改为客户端命令（MailClientCommand, src/client），补齐 MailRecallPayload/MailPurgePayload/MailListRequestPayload 3 个 C2S 包至 16 个；合并 Mail 模型并补 List<TargetSpec> targets 字段；权限措辞澄清 MailPermissionHelper 仅服务端；冒险经验引用 AdventureLevelManager.grantExp；新增多行编辑器风险/hidden 防呆风险项；包数量 13→16）

---

## 1. 目标与范围

### 1.1 定位
本系统是**单向、管理员到玩家**的「服务器信箱 / 公告箱」，不是玩家之间的私聊或物品交换系统。

| 维度 | 规划内容 |
|---|---|
| 发送方 | 仅管理员（OP / LuckPerms 节点） |
| 发布方式 | **管理员 GUI（`MailComposeScreen`），由命令 `/yzwc mail send_mail` 打开**；编辑邮件亦走 GUI（编辑模式）；撤回/清理为命令（见 §7） |
| 接收方 | 全体（含管理）/ 全体非管理 / 指定玩家 / 角色组；**可多选，取并集** |
| 内容类型 | 公告（ANNOUNCEMENT）、通知（NOTICE）、奖励（REWARD） |
| 奖励载体 | 物品、命令（含玩家名占位符）、原版经验值、原版等级、本项目冒险等级 |
| 后台管理 | 支持撤回 / 修改已发送邮件 |

### 1.2 明确不做（Out of Scope）
- 玩家 → 玩家 的私信、物品赠送、交易。
- 玩家自助撰写界面（仅管理员可发布）。
- 跨服务器（Bungee/Proxy）共享信箱（本期单服内存储即可）。
- 发布动作不使用命令传参（需求方明确：发布/编辑走 GUI，命令只负责打开界面）。

---

## 2. 总体架构

```
┌──────────────────────────────────────────────────────────────────┐
│ 客户端 (GUI)                                                       │
│  MainMenuElements(邮件按钮, Shift+F) ──打开──▶ MailScreen          │
│    MailScreen (玩家信箱)                                           │
│      ├─ [发布邮件] 按钮(仅管理员) ──打开──▶ MailComposeScreen      │
│      ├─ MailList (AbstractSelectionList, 可滚动)                  │
│      └─ 详情面板 / 领取·星标·删除                                   │
│  /yzwc mail send_mail ──(S2C OpenMailComposePayload)──▶           │
│  MailComposeScreen (管理员发布页 / 编辑模式复用)                   │
│      ├─ 接收范围(多选并集) / 主题 / 正文 / 附件勾选 / 过期          │
│      ├─ 物品附件: 最多 10 个槽位                                    │
│      └─ [发布] → C2S: MailAdminSendPayload / [保存修改] → MailAdminEditPayload │
│       │  C2S: MailOpenPayload / MailActionPayload / MailAdminSendPayload│
└───────┼───────────────────────────────────────────────────────────┘
        │  Fabric Networking v1 (CustomPacketPayload)
┌───────┼───────────────────────────────────────────────────────────┐
│ 服务端 (权威)                                                      │
│  ModNetworking (注册 + 处理器)                                     │
│       ▼                                                           │
│  MailManager (send / claim / recall / edit / expire / star)       │
│       ├─ SentMailRepository   (全局仓库: Map<mailId, Mail>)       │
│       ├─ MailDataStorage      (每玩家索引: box/<uuid>.json)       │
│       ├─ MailSettings         (config/.../mail_settings.json)    │
│       └─ LuckPermsHelper      (权限 / 非管理判定)                 │
│       │                                                           │
│  S2C: MailListPayload / MailUpdatePayload /                       │
│        MailOpResultPayload / MailUnreadCountPayload(canSend)      │
└──────────────────────────────────────────────────────────────────┘
        ▲
        │  /yzwc mail send_mail (打开发布GUI) / /yzwc mail recall|purge|list (编辑改 GUI)
```

---

## 3. 数据模型

### 3.1 `MailType`（枚举）
```java
public enum MailType {
    ANNOUNCEMENT,  // 公告
    NOTICE,        // 通知
    REWARD         // 含奖励
}
```

### 3.2 `MailAttachment`（奖励附件，5 种类型）
```java
public record MailAttachment(
    AttachmentType type,
    String data,    // ITEM: NBT串; COMMAND: 命令串; 其余: 数值字符串
    int amount,     // 数量 / 点数 / 等级
    @Nullable String itemNbt  // 仅 ITEM 使用: ItemStack 的 NBT 序列化
) {}
```
附件类型与发放方式：
- **ITEM（物品）**：由管理员在 GUI 中从自身物品栏选取的 `ItemStack`，序列化为 NBT 存储。**单封邮件最多 10 个物品附件**（`MailComposeScreen` 预留 10 个槽位）。领取时服务端逐个据 NBT 重建 `ItemStack` 并 `player.getInventory().add(...)`；背包满则掉落为 `ItemEntity`。**管理员原物品不被消耗**（仅作模板复制）。
- **COMMAND（指令）**：以控制台身份在领取时执行，支持占位符 `%player%`（接收者名）、`%uuid%`（接收者 UUID）。
- **VANILLA_EXP（原版经验值）**：`player.giveExperiencePoints(amount)`。
- **VANILLA_LEVEL（原版等级）**：`player.giveExperienceLevels(amount)`。
- **ADVENTURE_EXP（本项目冒险等级）**：经 `AdventureLevelManager.grantExp(ServerPlayer, int)` 在领取时增加对应玩家冒险经验（该调用已自带 HUD 同步）。

> 网络侧物品用 `ItemStack.OPTIONAL_STREAM_CODEC` 写入 `RegistryFriendlyByteBuf`；落盘用 NBT 字符串。

### 3.3 `Mail`（服务端权威对象，存于全局仓库）
```java
public record Mail(
    UUID id,
    MailType type,
    String sender,
    List<TargetSpec> targets,   // 原始接收范围（编辑预填 + 范围 diff 用）
    String title,
    String body,
    long createdTime,
    @Nullable Long expireTime,   // null = 永久
    List<MailAttachment> attachments
) {}
```

### 3.4 存储模型：全局仓库 + 每玩家索引（支持撤回/修改）
- **`SentMailRepository`（全局仓库）**：`Map<UUID mailId, Mail>`，持久化于 `config/youzaiworldcore/mail/sent.json`（或 `sent/<mailId>.json`）。
- **`PlayerMailbox`（每玩家索引）**：仅存引用与本地状态，不含正文：
  ```java
  public class PlayerMailbox { UUID playerUuid; List<MailRef> mails; }
  public class MailRef { UUID mailId; boolean read; boolean starred; boolean claimed; }
  ```
- `Mail` 统一为单一类（替代 §3.3 record），包含声明字段与运行时状态：
  ```java
  public class Mail {
      UUID id; MailType type; String sender; List<TargetSpec> targets;
      String scopeSummary;     // 展示用范围摘要（如「全体+指定Steve」）
      String title; String body;
      long createdTime; Long expireTime; // null = 永久
      boolean claimed;         // 是否已有任意接收者领取过奖励
      boolean hidden;          // 编辑中隐藏标志
      List<MailAttachment> attachments;
  }
  ```
- 渲染/领取时由 `mailId` 从仓库取正文；取不到（已撤回）→ 加载时自动剔除。
- **撤回**：删仓库条目；在线接收者推送移除；离线接收者登录自动丢弃（无需墓碑）。
- **修改**：更新仓库 `Mail` 并保存，在线接收者推送 `MailUpdatePayload`。

---

## 4. 持久化方案

### 4.1 存储位置
- 全局仓库：`config/youzaiworldcore/mail/sent.json`。
- 每玩家索引：`config/youzaiworldcore/mail/box/<player-uuid>.json`。
- 全局设置：`config/youzaiworldcore/mail_settings.json`。

### 4.2 `SentMailRepository` / `MailDataStorage`
- 均使用 `ConcurrentHashMap` + 读写锁，仿 `AccountDataStorage` 模式，全程 `DebugLogger`。
- `MailDataStorage.load` 时剔除正文缺失（已撤回）与过期未星标引用。

### 4.3 跨世界一致性
数据位于全局 config 目录（非某存档 `SavedData`），与 `AccountDataStorage`、`PlayerLevelStorage` 一致。

---

## 5. 过期与收藏机制

### 5.1 过期判定
- 计算：`expired = expireTime != null && System.currentTimeMillis() > expireTime`。
- 过期时间**可选项锁定**为：`1天` / `7天` / `30天` / `永久`；默认 `30天`。
- 清理策略：过期**未星标** → 删除；过期**已星标** → 保留文本、禁用奖励领取（需求确认口径）。

### 5.2 触发时机
- 加载即清理；周期性扫描（每 3000 tick）清理在线玩家；离线玩家登录时清理。

### 5.3 配置项
- `default_expire`：`"30d"`；`expire_options`：`["1d","7d","30d","permanent"]`；`keep_starred_after_expire`：`true`；`max_mails_per_player`：200。

---

## 6. 奖励发放与领取

### 6.1 打开发布页与发布（命令打开 GUI）
- **打开**：管理员执行 `/yzwc mail send_mail`（`MailCommand` 子命令，需统一邮件权限 `mail_permission_node`）。服务端校验权限后，向该管理员下发 `OpenMailComposePayload`（S2C），客户端收到即 `setScreenAndShow(new MailComposeScreen())`。（客户端命令→C2S `MailComposeOpenPayload`→服务端权限校验→S2C 打开界面，符合项目「客户端命令仅解析+发包、服务端持权威」架构；亦可直接在客户端命令回调内本地打开界面，但为统一权限校验推荐经服务端确认后 S2C 打开。）
- **发布**：管理员在 `MailComposeScreen` 填写表单并点「发布」→ 客户端组装 `MailAdminSendPayload` 发送。

服务端 `MailManager.send(...)`：
1. 服务端**二次校验权限**（统一邮件权限 `mail_permission_node`，不信任客户端）。
2. 解析接收者集合（`resolveTargets`，见 §7.3）：**对多个已选范围分别解析后取并集去重**（按 UUID 去重，含离线玩家）。
3. 构造 `Mail`（含 ≤10 物品附件、其他附件、expireTime），写入 `SentMailRepository`。
4. 为并集中每位接收者 `addRef(...)`，保存。
5. 在线接收者推送 `MailUpdatePayload` + 更新未读徽标 `MailUnreadCountPayload`。

### 6.2 玩家领取
客户端「领取」→ `MailActionPayload(CLAIM, mailId)`：
- 服务端校验存在/REWARD/未领取/未过期 → 逐个附件发放（ITEM/COMMAND[替换占位符]/VANILLA_EXP/VANILLA_LEVEL/ADVENTURE_EXP）→ 标记 `claimed` → 回 `MailOpResultPayload`。
- 过期/已撤回邮件：按钮禁用 + 服务端拒绝。

### 6.3 撤回 / 修改
- **撤回**：`/yzwc mail recall <mailId>` 命令（需统一邮件权限）→ 删仓库条目，在线推送移除，离线登录丢弃。
- **修改（GUI）**：管理员在「已发送邮件」管理列表（`MailSentScreen`，见 §9.6，由信箱内 [已发送邮件] 按钮或 `/yzwc mail sent` 打开）点击某封邮件的 [编辑] → 客户端先发 `MailFetchPayload(mailId)` 拉取完整字段，**服务端在回包（`MailUpdatePayload` 单封形式）中附带 `canEdit` 标志**；客户端据 `canEdit` 决定能否进入编辑模式：
  - **`canEdit == false`**：编辑器不可编辑，仅展示邮件内容，并提示「已有玩家领取过附件，不可编辑，仅可撤回」+ [撤回] 按钮（走 `recall` 命令）。
  - **`canEdit == true`**：以编辑模式打开 `MailComposeScreen` 预填字段 → 改完点 [保存修改] → `MailAdminEditPayload(mailId, 新字段)`。
  - 服务端 `MailManager.edit(...)`：二次校验权限（统一邮件权限）、校验 `mailId` 仍存在、合并可改字段（scope/目标/title/body/expire/附件）、重建附件、写回 `SentMailRepository` 并保存，向在线接收者推送 `MailUpdatePayload`。
- **编辑前置规则（服务端 `canEdit` 计算，`MailManager.computeCanEdit`）**：
  1. 邮件**无附件**（`attachments.isEmpty()`，即公告/通知类）→ `canEdit = true`，可随时编辑。
  2. 邮件**有附件**（`REWARD`）：
     - `mail.claimed == true`（已有人领取过奖励，见 §6.2，`SentMailRepository.Mail.claimed` 由 `MailManager.claim` 置 `true` 且不再回退）→ `canEdit = false`：**不允许编辑，仅可撤回**（避免已发奖励被篡改或追回争议）。
     - `mail.claimed == false` → `canEdit = true`，但编辑期间需**隐藏**：进入编辑时设 `mail.hidden = true` 并向在线接收者推 `MailUpdatePayload(隐藏/移除)`，使玩家信箱不再显示该邮件；编辑完成（`MailAdminEditPayload` 应用后）设 `mail.hidden = false` 并重新推送，玩家信箱恢复显示。若编辑中途取消，客户端发 `MailAdminEditPayload(mailId, cancel=true)`，服务端仅恢复 `hidden=false`（不改动任何字段）。
  - **范围变更处理**（仅 `canEdit` 通过时）：编辑后 `targets` 改变重新 `resolveTargets` 得新全集；对**新增** UUID `addRef`（未读）；对**被移除** UUID，若其 `MailRef.claimed==false` 则删除其 ref（等同撤回该接收者），若 `claimed==true` 则保留（**不追回已发奖励**）。
  - 已过期邮件仍可改文本但领取按钮维持禁用；`hidden` 字段随完整 `Mail` 经 `MailUpdatePayload` 下发，客户端据此决定是否在信箱列表渲染该邮件。

---

## 7. 命令与权限

### 7.1 命令（发布 GUI 入口 + 管理命令）
- `/yzwc mail` 整棵命令树统一要求邮件权限（`mail_permission_node`，见 §7.2），不再区分 `send`/`admin` 子节点。
- **打开发布 GUI**：`/yzwc mail send_mail`（`MailCommand` 子命令）→ 经 `MailComposeOpenPayload` + `OpenMailComposePayload` 打开 `MailComposeScreen`。
- **打开已发送管理列表**：`/yzwc mail sent` → 打开 `MailSentScreen`（信箱内 [已发送邮件] 按钮同效，二者均依赖 `canSend` 即统一邮件权限）。
- 其余管理命令（`MailCommand`）：
```
/yzwc mail recall <mailId>          撤回已发送邮件
/yzwc mail purge [player|all]        立即清理过期邮件
/yzwc mail list [player]             查看某玩家信箱
```
> 发布与编辑邮件的表单填写一律在 GUI 完成（不用命令传参）：`send_mail` 命令只负责打开空白发布页，`sent` 命令/按钮打开已发送列表，[编辑] 在列表内以 GUI 编辑模式打开预填表单；`recall`/`purge`/`list` 仍为命令。

### 7.2 权限（`MailPermissionHelper`，服务端判定）
- **mail 为客户端命令**（`MailClientCommand`），不通过服务器 `.requires()` 预过滤；权限检查全部在服务端 C2S 包处理器中完成。
- **单一权限节点**：`mail_permission_node`（默认 `youzaiworldcore.mail`）。所有邮件相关 C2S 包的服务端处理器（`MailComposeOpenPayload`/`MailAdminSendPayload`/`MailAdminEditPayload`/`MailRecallPayload`/`MailPurgePayload`/`MailListRequestPayload`/`MailFetchPayload`）均统一校验该节点。
- **OP 回退**：未装 LuckPerms 时，以 `mail_permission_level`（默认 4）为 OP 等级阈值。
- **`MailPermissionHelper.hasMailPermission(ServerPlayer)`**（服务端专用，客户端无 LP API）：
  ```java
  public static boolean hasMailPermission(ServerPlayer player) {
      return LuckPermsHelper.checkLuckPermsOnly(player.getUUID(), MailSettings.get().mailPermissionNode)
          || player.hasPermission(MailSettings.get().mailPermissionLevel);
  }
  ```
  `checkLuckPermsOnly` 仅在 LP 安装时生效，LP 未安装返回 `false`，由 `player.hasPermission(int)` 回退到原版 OP 等级判定。
- **`canSend`（客户端按钮显隐）**：`MailUnreadCountPayload` 的 `canSend` 字段由服务端在登录时计算并下发，客户端仅信任该值，不自算权限（客户端无 LuckPerms API）。校验模式参照 `ModNetworking` 中其他 C2S 处理器（如 `InvisibilityPayload`）的权限校验写法。

### 7.3 接收范围（多选并集）与「非管理」判定
- **接收范围支持多选**：GUI 中每个范围为一个可勾选项，可同时勾选多个；服务端把各范围解析出的玩家集合**取并集并按 UUID 去重**。例如「全体非管理 + 指定玩家 Steve」= 非管理玩家 ∪ {Steve}。
- 可选范围：
  - `ALL` 全体（含管理）
  - `NONADMIN` 全体非管理
  - `PLAYER` 指定玩家（可填多个玩家名，逗号/空格分隔）
  - `ROLE` 角色组 / 权限节点（可填多个节点）
- `nonadmin` 判定：不持有邮件权限（即 `!MailPermissionHelper.hasMailPermission(account)`，未持 `mail_permission_node` 且 OP 等级低于 `mail_permission_level`）的玩家。
- **与账户系统挂钩（离线覆盖策略，已定）**：`ALL` / `NONADMIN` / `ROLE` 的接收者全集**以账户系统中全部已注册账户为准**——经 `AccountDataStorage.getAll()` 枚举 `PlayerAccount`（含 `uuid`）作为全集，而非仅在线或仅曾登录玩家。离线账户同样写入其收件箱索引，待其登录时 `MailScreen` 从 `SentMailRepository` 拉取正文即可见。`PLAYER`（指定玩家）仍按名→`uuid` 解析（`AccountDataStorage.get(name).uuid` 或 `UserCache`）。这天然解决离线覆盖问题：只要账户存在即纳入范围。
- **账户注销时清空邮件**：账户注销（自注销 `/yzwc account deactivate` 与管理员删除 `/yzwc account mgr delete`，均在 `account/command/AccountCommands.java`）成功后，调用 `MailManager.onAccountDeleted(uuid)` → `MailDataStorage.removeBox(uuid)` 删除该账户收件箱索引（含未读/已收藏/已领取状态）。其作为发送者已投递的邮件保留在 `SentMailRepository`（已送达，不级联删除）。self 路径用 `player.getUUID()` 取 uuid；admin 路径先 `AccountDataStorage.get(name)` 取 uuid 再删除（集成点见 §10）。
- `MailManager.resolveTargets(List<TargetSpec>)` 返回去重后的 `Set<UUID>` + 在线 `ServerPlayer` 映射；全集即 `AccountDataStorage.getAll()` 的 uuid 集合。

---

## 8. 网络协议（Fabric Networking v1）

### 8.1 数据包清单
| 包 | 方向 | 字段 |
|---|---|---|
| `MailComposeOpenPayload` | C2S | (空) — `/yzwc mail send_mail` 触发，请求打开发布页 |
| `OpenMailComposePayload` | S2C | (空) — 服务端权限校验通过后令客户端打开 `MailComposeScreen` |
| `MailOpenPayload` | C2S | (空) — 打开信箱时请求收件箱列表 |
| `MailSentListRequestPayload` | C2S | (空) — 请求「已发送邮件」列表 |
| `MailRecallPayload` | C2S | `mailId` — 撤回已发送邮件 |
| `MailPurgePayload` | C2S | `target`(String) — `"all"` 或玩家名，清理过期邮件 |
| `MailListRequestPayload` | C2S | `targetPlayer`(String) — 管理员查看指定玩家的信箱 |
| `MailFetchPayload` | C2S | `mailId` — 编辑前请求单封完整 `Mail` |
| `MailActionPayload` | C2S | `mailId`, `action`(OPEN/READ/STAR/UNSTAR/CLAIM/DELETE) |
| `MailAdminSendPayload` | C2S | `List<TargetSpec> targets`, `type`, `title`, `body`, `expireOption`(byte), `List<AttachmentData>` |
| `MailAdminEditPayload` | C2S | `mailId`, `List<TargetSpec> targets`, `type`, `title`, `body`, `expireOption`(byte), `List<AttachmentData>` |
| `MailListPayload` | S2C | `List<MailRef+Mail>`（收件箱） |
| `MailSentListPayload` | S2C | `List<MailSummary>`（已发送：id/title/scope摘要/sendTime/到期） |
| `MailUpdatePayload` | S2C | 单封 `MailRef+Mail`（增/改）或 `removedMailId` |
| `MailOpResultPayload` | S2C | `mailId`, `success`, `reason` |
| `MailUnreadCountPayload` | S2C | `unreadCount`, `canSend`(bool) |

**总计：10 C2S + 6 S2C = 16 个包。**

`TargetSpec`（包内结构）：`byte scope`(0=ALL,1=NONADMIN,2=PLAYER,3=ROLE) + `List<String> args`（PLAYER/ROLE 时为名称/节点列表，可多项；ALL/NONADMIN 时忽略）。用 `List<String>` 而非分隔符拼接，避免玩家名/节点含逗号或空格的转义歧义。`targets` 列表可含多项，服务端取并集。
`MailSummary`（包内结构，用于 `MailSentListPayload`）：`UUID mailId`、`MailType type`、`String title`、`String scopeSummary`（如「全体+指定 Steve」）、`long sendTime`、`@Nullable Long expireTime`、`String sender`。
`AttachmentData`（包内结构）：`byte type` + `String data` + `int amount` + `@Nullable ItemStack item`（ITEM 类型携带，用 `ItemStack.OPTIONAL_STREAM_CODEC`）。**ITEM 类型的 `AttachmentData` 在列表中最多 10 项**（服务端强制校验，超出拒绝）。
`expireOption` 编码：`0=1d, 1=7d, 2=30d, 3=permanent`。

### 8.2 注册位置
- 服务端：`ModNetworking.initialize()`（类型注册 + 处理器）：
  - `MailComposeOpenPayload` → 校验统一邮件权限 `mail_permission_node` → 回 `OpenMailComposePayload`。
  - `MailAdminSendPayload` → 校验 `mail_permission_node` → `resolveTargets` 取并集 → `MailManager.send`。
  - `MailAdminEditPayload` → 校验 `mail_permission_node` → `MailManager.edit` → 推送 `MailUpdatePayload`；（`cancel=true` 时仅恢复 `hidden=false` 不改动字段）。
  - `MailSentListRequestPayload` → 校验 `mail_permission_node` → 回 `MailSentListPayload`（列表直接显示全部已发送邮件）。
  - `MailRecallPayload` → 校验 `mail_permission_node` → `MailManager.recall(mailId)` → 删仓库条目 + 推送在线接收者移除。
  - `MailPurgePayload` → 校验 `mail_permission_node` → `MailManager.purge(target)`（`"all"` 或特定玩家）→ 清理过期未星标邮件。
  - `MailListRequestPayload` → 校验 `mail_permission_node` → 查询目标玩家信箱 → 回 `MailListPayload`（管理员查看）。
  - `MailFetchPayload` → 校验 `mail_permission_node` → 从 `SentMailRepository` 取完整 `Mail` 回 `MailUpdatePayload`（单封完整形式，含 `canEdit`/`hidden` 字段）；不存在回 `MailOpResultPayload(not_found)`。
- 客户端：`ClientNetworking.initialize()`：
  - `OpenMailComposePayload` → `setScreenAndShow(new MailComposeScreen())`（新建）。
  - `MailSentListPayload` → 打开/刷新 `MailSentScreen`。
  - `MailListPayload`/`MailUpdatePayload`/`MailOpResultPayload` → 刷新 `MailScreen`。
  - `MailUnreadCountPayload` → 设置 `MailClientState.unreadCount` 与 `canSend`（控制发布/已发送按钮显隐）。

---

## 9. 客户端 GUI

### 9.1 入口（玩家信箱）
- `MainMenuElements.java` 第 188–228 行，将 `i == 1`（邮件）的 `onClick` 改为打开 `MailScreen`：
  ```java
  onClick = () -> Minecraft.getInstance().setScreenAndShow(new MailScreen());
  ```
- 未读徽标：`MailClientState.unreadCount` 由 `MailUnreadCountPayload` 更新；`MainMenuElements` 绘制邮件按钮时叠加红点+数字。

### 9.2 `MailScreen`（玩家信箱，`extends Screen`，标准渲染管线）
- 顶部：标题「邮件」+ 筛选标签（全部 / 未读 / 已收藏）。
- 中部：`MailList extends AbstractSelectionList<MailList.MailEntry>` 滚动列表（项目无现成滚动控件，用 MC 原生 `AbstractSelectionList`）。
- 详情面板：类型、发件人、时间、正文多行滚动、过期倒计时/「已过期」、附件预览、按钮（领取/星标/删除/标记已读）。
- 底部：批量操作（删除全部已读、清理过期）。
- **右上角 [发布邮件] 按钮**：仅当 `MailClientState.canSend == true` 时显示，点击发送 `MailComposeOpenPayload` 打开空白 `MailComposeScreen`。
- **右上角 [已发送邮件] 按钮**：仅当 `MailClientState.canSend == true`（即持有邮件权限者）时显示，点击发送 `MailSentListRequestPayload` 打开 `MailSentScreen`（见 §9.6）。

### 9.3 可滚动列表控件
`MailList extends AbstractSelectionList<MailList.MailEntry>`，`MailEntry` 持有 `MailRef+Mail` 并重写 `render` 绘制单行（类型图标/标题/发件人/日期/未读圆点/星标/附件标记）。

### 9.4 客户端状态 `MailClientState`
- `static int unreadCount`、`static boolean canSend`、`static List<MailRefAndMail> currentInbox`。

### 9.5 `MailComposeScreen`（管理员发布页，`extends Screen`）★本期新增
复用项目已有控件：`DropdownButton`（下拉）、`CheckboxButton`（勾选框）、`EditBox`（文本输入）、`TransparentButton`（按钮）。

**打开方式**：仅由 `/yzwc mail send_mail` 命令打开（服务端统一邮件权限校验通过后 S2C 触发空白新建）；玩家信箱内的 [发布邮件] 按钮亦走同一请求路径（`MailComposeOpenPayload`）。编辑模式由 `MailSentScreen` 的 [编辑] 按钮触发：`MailFetchPayload` 拉取完整字段 → 服务端回 `MailUpdatePayload` 带 `canEdit` → 客户端据 `canEdit` 决定是否进入可编辑的预填 `MailComposeScreen`（见 §9.5 末尾「编辑模式」与 §6.3 前置规则）。

**界面布局（按需求方草图，含多选范围 + 10 物品槽）**：
```
┌───────────────────────────────────────────────────────────┐
│  发布邮件                                        [取消][发布] │
├───────────────────────────────────────────────────────────┤
│  接收范围(可多选,取并集)：                                   │
│    ☑ 全体   ☐ 全体非管理   ☑ 指定玩家   ☐ 角色组           │
│      指定玩家(逗号分隔): [Steve, Alex___________]           │
│      角色组/节点(逗号分隔): [__________________]            │
│  类型：    [下拉: 奖励▾]  （公告 / 通知 / 奖励）            │
│  主题：    [_________________________]                     │
│  文本正文： ┌─────────────────────────────┐                │
│            │ 多行文本区域（可滚动）        │                │
│            └─────────────────────────────┘                │
│  附加附件：                                                 │
│   ☑ 物品附件（最多10个）                                    │
│       ┌─┬─┬─┬─┬─┬─┬─┬─┬─┬─┐  ← 10 个附件槽(点击填入)      │
│       └─┴─┴─┴─┴─┴─┴─┴─┴─┴─┘                              │
│       [从我的物品栏选取]  选中槽数量:[__]                   │
│   ☑ 指令附件                                               │
│       指令（用 %player% 代替玩家名称）：[_____________]     │
│   ☑ 给予原版经验值    经验值：[__]                         │
│   ☑ 增加等级          等级值：[__]                         │
│   ☑ 冒险等级          冒险等级：[__]                       │
│  过期时间： [下拉: 30天▾]  （1天/7天/30天/永久）           │
└───────────────────────────────────────────────────────────┘
```

**交互细节**：
- **接收范围（多选并集）**：4 个 `CheckboxButton`（全体 / 全体非管理 / 指定玩家 / 角色组），可同时勾选多个；勾选「指定玩家」「角色组」时分别显示 `EditBox`（支持逗号/空格分隔的多个名称/节点）。发布时把所有勾选项打包为 `List<TargetSpec>`，服务端取并集去重。
- **类型（必填）**：`DropdownButton`（公告 / 通知 / 奖励）。**仅 `REWARD`（奖励）需要附件**；选 `ANNOUNCEMENT`/`NOTICE` 时禁用「附加附件」分区（或其勾选无效），发布/保存时不校验附件。该值写入 `MailAdminSendPayload`/`MailAdminEditPayload` 的 `type` 字段。
- **主题 / 正文**：`EditBox`；正文用多行可滚动文本区（基于 `EditBox` 扩展或自研滚动文本控件，支持 `\n`）。
- **附件勾选**：每个 `CheckboxButton` 勾选后显示对应输入行；可组合多个附件。
- **物品附件（≤10 槽）**：勾选后显示一排 **10 个附件槽位**。点击某槽 → 弹出/切换到「从我的物品栏选取」模式，渲染管理员自身**物品栏**（主手/副手 + 9 热栏槽，或全 36 格背包）为可点击源；点击源物品即把该 `ItemStack` **复制**为该附件槽的模板（管理员原物不消耗），每槽可单独设数量。已填槽可点击清除。最多 10 槽，超出禁止再加。
- **指令附件**：`EditBox`，提示「用 %player% 代替玩家名称」。
- **经验/等级/冒险等级**：各自 `EditBox` 填数值。
- **过期时间**：`DropdownButton`，默认 `mail_settings.default_expire`（30天）。
- **[发布]**（新建模式）：客户端组装 `MailAdminSendPayload`（`List<TargetSpec>` / `type` / title / body / expireOption / 附件列表[物品经 `ItemStack.OPTIONAL_STREAM_CODEC` 编码，ITEM≤10]），发送；服务端校验权限、至少勾选 1 个接收范围、主题非空、`type==REWARD` 时至少 1 附件、物品附件≤10 后入库，回 `MailOpResultPayload`；成功后关闭本屏。
- **[保存修改]**（编辑模式）：复用同一界面，按钮文案变为「保存修改」；客户端组装 `MailAdminEditPayload(mailId + 与发布相同的可改字段)` 发送；服务端 `MailManager.edit` 校验后写回仓库并推送 `MailUpdatePayload`，成功后关闭本屏。
- **[取消]**：关闭本屏（返回上一界面或游戏）。

**编辑模式（GUI 编辑已发送邮件）**：
- 进入：管理员在 `MailSentScreen`（§9.6）点击某封邮件的 [编辑] → 客户端发 C2S `MailFetchPayload(mailId)` → 服务端据 `mailId` 从 `SentMailRepository` 取完整 `Mail`（含附件/item NBT），以 `MailUpdatePayload`（单封完整形式）回传 → 客户端据返回数据构造编辑模式 `MailComposeScreen` 并预填：类型、接收范围（反解 `Mail` 的 scope 描述）、主题、正文、过期选项、各附件（物品附件用 `ItemStack.OPTIONAL_STREAM_CODEC` 解码回 10 槽模板）。若 `mailId` 在仓库已不存在（已被撤回），回 `MailOpResultPayload(success=false, reason=not_found)` 并提示。
- 受信字段：编辑模式下 `mailId` 不可改；接收范围/目标/主题/正文/过期/附件均可改；可改附件组合（如新增/删除物品槽、改命令、改经验值）。
- 发送即覆盖原 `Mail`：`SentMailRepository` 更新条目（保留 `id`/`createdTime`/`sender`），在线接收者推送 `MailUpdatePayload` 刷新其信箱；已领取状态 `claimed` 保留（不回退）。
- 权限：仅持有统一邮件权限者（邮件系统 OP/权限组成员）可在 `MailSentScreen` 看到 [编辑] 并成功发送 `MailAdminEditPayload`（服务端二次校验权限 + `canEdit` 规则）。

**校验（客户端轻量 + 服务端权威）**：
- 客户端：主题非空、勾选的附件其输入合法（如物品已选、数值为正整数）。
- 服务端：重做权限校验（统一邮件权限 `mail_permission_node`）+ 附件合法性（命令以 `/` 开头、无危险重定向；物品 NBT 可解析）+ 防刷（如单封附件数量上限）。

### 9.6 `MailSentScreen`（管理员「已发送邮件」管理列表，`extends Screen`）★本期新增
- **打开**：持有统一邮件权限即可（[已发送邮件] 按钮在 `canSend==true` 时显示）。入口：玩家信箱内 [已发送邮件] 按钮，或命令 `/yzwc mail sent`。客户端发 `MailSentListRequestPayload`，服务端校验 `mail_permission_node` 后回 `MailSentListPayload`（邮件系统全由权限组使用，列表显示全部已发送邮件），客户端据此构造列表。
- **列表控件**：`MailSentList extends AbstractSelectionList<MailSentList.SentEntry>`，每行展示：主题、接收范围摘要（如「全体+指定 Steve」）、发送时间、到期状态、右侧 [编辑] / [撤回] 按钮。
- **[编辑]**：见 §9.5「编辑模式」——以 `mailId` 拉取完整字段、预填 `MailComposeScreen`、[保存修改] 发 `MailAdminEditPayload`。
- **[撤回]**：客户端命令 `/yzwc mail recall <mailId>` 发送 C2S `MailRecallPayload`，服务端 `MailManager.recall(mailId)` 执行。列表中同时展示该邮件完整 `mailId`（tooltip 可复制）；也可在此屏直接添加 [撤回] 按钮，点击发送 `MailRecallPayload` 即可（扩展点：按钮→`ClientPlayNetworking.send(new MailRecallPayload(mailId))`）。
- **刷新**：`MailSentListPayload` 重新下发时重建列表；编辑/撤回成功后由服务端回包或再次请求刷新。

---

## 10. 与现有代码的集成点

| 文件 | 改动 |
|---|---|
| `src/client/.../client/screen/element/MainMenuElements.java` | 邮件按钮 `onClick` 改为打开 `MailScreen`；绘制未读徽标 |
| `src/main/.../network/ModNetworking.java` | 注册 16 个包 + 处理器（`MailComposeOpenPayload`→校验→`OpenMailComposePayload`；`MailAdminSendPayload`→校验→并集→`MailManager.send`；`MailAdminEditPayload`→校验→`MailManager.edit`；`MailSentListRequestPayload`→校验→`MailSentListPayload`；`MailRecallPayload`→校验→`MailManager.recall`；`MailPurgePayload`→校验→`MailManager.purge`；`MailListRequestPayload`→校验→回 `MailListPayload`；`MailFetchPayload`→校验→`MailUpdatePayload` 单封） |
| `src/client/.../network/ClientNetworking.java` | 注册 S2C 接收器；`OpenMailComposePayload`→打开发布页；`MailSentListPayload`→打开/刷新 `MailSentScreen`；`MailUnreadCountPayload` 设 `canSend`；打开/刷新 `MailScreen` |
| `src/client/.../client/Client.java` | `onInitialize`：`MailClientCommand.register()`、`MailSettings.load()`、`SentMailRepository.initialize()`、`MailDataStorage.initialize()`；**注册玩家登录监听器**：登录时向该玩家推送 `MailUnreadCountPayload`(未读+canSend) 并触发其信箱加载清理（过期/已撤回剔除） |
| `src/client/.../command/MailClientCommand.java` | 客户端命令：`send_mail`(→C2S MailComposeOpenPayload) / `sent`(→MailSentListRequestPayload) / `recall <mailId>`(→MailRecallPayload) / `purge [player|all]`(→MailPurgePayload) / `list [player]`(→MailListRequestPayload)，均仅解析转发；服务端 `ModNetworking` 处理器执行 |
| `src/main/.../account/command/AccountCommands.java` | 两处账户删除逻辑（`deactivate` 自注销 / `executeAdminDelete` 管理员删除）成功后调用 `MailManager.onAccountDeleted(uuid)` → `MailDataStorage.removeBox(uuid)` 清空该账户收件箱 |
| 语言文件 `*.json` | 新增邮件相关 key（含发布页、编辑模式、已发送列表、多选范围、send_mail/sent 命令反馈） |

新建文件：
- `src/main/.../mail/`：`Mail.java` `MailType.java` `MailAttachment.java` `MailRef.java` `TargetSpec.java` `SentMailRepository.java` `MailDataStorage.java` `MailManager.java` `MailSettings.java`
- `src/main/.../network/`：`MailComposeOpenPayload` `OpenMailComposePayload` `MailOpenPayload` `MailSentListRequestPayload` `MailRecallPayload` `MailPurgePayload` `MailListRequestPayload` `MailFetchPayload` `MailActionPayload` `MailAdminSendPayload` `MailAdminEditPayload` `MailListPayload` `MailSentListPayload` `MailUpdatePayload` `MailOpResultPayload` `MailUnreadCountPayload`
- `src/client/.../client/screen/`：`MailScreen.java` `MailComposeScreen.java` `MailSentScreen.java`
- `src/client/.../client/screen/widget/`：`MailList.java` `MailSentList.java`
- `src/client/.../client/MailClientState.java`
- `src/client/.../command/MailClientCommand.java`

---

## 11. 配置文件 `mail_settings.json`
```json
{
  "default_expire": "30d",
  "expire_options": ["1d", "7d", "30d", "permanent"],
  "keep_starred_after_expire": true,
  "max_mails_per_player": 200,
  "auto_purge_interval_ticks": 3000,
  "mail_permission_level": 4,
  "mail_permission_node": "youzaiworldcore.mail",
  "max_item_attachments": 10,
  "max_attachments_per_mail": 16
}
```
> `max_item_attachments` 固定上限 10（对应发布页 10 个物品槽）；`max_attachments_per_mail` 为含所有类型的总上限。
> `mail_permission_level` 仅在 LuckPerms 未安装时作为 OP 等级回退阈值（默认 4）；装了 LuckPerms 则以 `mail_permission_node` 节点为准，该字段不参与节点判定。邮件系统整体仅由持有该权限的 OP/权限组成员使用。

---

## 12. 本地化 Key（建议，含发布页）
```
youzaiworldcore.message.gui.mail.title
youzaiworldcore.message.gui.mail.unread / starred / claim / claimed / expired / empty
youzaiworldcore.message.gui.mail.compose.title          // 发布邮件
youzaiworldcore.message.gui.mail.compose.scope          // 接收范围
youzaiworldcore.message.gui.mail.compose.scope.all / nonadmin / player / role
youzaiworldcore.message.gui.mail.compose.subject        // 主题
youzaiworldcore.message.gui.mail.compose.body           // 文本正文
youzaiworldcore.message.gui.mail.compose.attachments    // 附加附件
youzaiworldcore.message.gui.mail.compose.item / cmd / vanilla_exp / vanilla_level / adventure_level
youzaiworldcore.message.gui.mail.compose.expire         // 过期时间
youzaiworldcore.message.gui.mail.compose.publish / cancel
youzaiworldcore.message.gui.mail.compose.hint_cmd       // 用 %player% 代替玩家名称
youzaiworldcore.message.command.mail.recalled / no_permission / expired_cannot_claim / target_not_found
youzaiworldcore.message.command.mail.send_mail.opened         // 已打开发布界面
youzaiworldcore.message.command.mail.send_mail.no_permission
youzaiworldcore.message.command.mail.sent.opened              // 已打开已发送邮件列表
youzaiworldcore.message.command.mail.sent.no_permission
youzaiworldcore.message.gui.mail.sent.title                   // 已发送邮件
youzaiworldcore.message.gui.mail.sent.edit / recall           // 编辑 / 撤回
youzaiworldcore.message.gui.mail.sent.copy_id                 // 点击复制邮件ID
youzaiworldcore.message.gui.mail.compose.edit_title          // 编辑邮件（编辑模式标题）
youzaiworldcore.message.gui.mail.compose.save                // 保存修改
youzaiworldcore.message.gui.mail.compose.no_scope_selected    // 请至少选择一个接收范围
youzaiworldcore.message.gui.mail.compose.too_many_items       // 物品附件最多 10 个
youzaiworldcore.message.gui.mail.compose.cannot_edit_claimed // 已有玩家领取过附件，不可编辑，仅可撤回
youzaiworldcore.message.gui.mail.compose.editing_hidden       // 编辑中：接收者暂不可见，保存后恢复
```

---

## 13. 风险、边界与备选

1. **仓库模型性能**：全服发文写 N 个轻量索引 + 1 仓库条目；超大服可改 `sent/<mailId>.json` 每封一文件。
2. **背包满**：ITEM 领取满则掉落 `ItemEntity` 并提示。
3. **命令奖励安全**：`COMMAND` 以控制台执行，解析阶段白名单校验（仅 `/` 开头、禁重定向），占位符由服务端替换防注入。
4. **物品模板不消耗**：GUI 选取仅复制 `ItemStack` 作模板，管理员物品栏原物保留；服务端据 NBT 重建发放。
5. **离线覆盖与账户绑定**：`ALL`/`NONADMIN`/`ROLE` 以 `AccountDataStorage.getAll()` 全部已注册账户为全集解析，离线账户同样入索引、登录可见（见 §7.3）。账户注销时 `MailManager.onAccountDeleted` 清空其收件箱（集成点见 §10）；严格按时过期可加每日定时任务（备选）。
6. **UUID 主键**：兼容改名。
7. **AWT/Headless**：纯 GUI + 命令，不触碰文件对话框（26.2 Headless 限制，见工作记忆），无冲突。
8. **冒险等级发放**：`ADVENTURE_EXP` 在领取时调用 `AdventureLevelManager.grantExp(ServerPlayer, int)`，该调用自带 HUD 同步。确认该方法存在（`grantExp(ServerPlayer, int)`，`AdventureLevelManager.java:141`）。
9. **正文多行编辑器**：项目无现成多行滚动 `EditBox`，`MailComposeScreen` 的「文本正文」输入区需自研（基于 `EditBox` 扩展或自建滚动文本控件），P5 需预留额外工作量。
10. **编辑期 hidden 超时防呆**：管理员进入编辑态时 `mail.hidden=true` 使接收者不可见。若管理员断线/超时/未保存，邮件会永久隐藏。需在 `MailManager` 添加超时自动恢复机制（如 5 分钟定时器，或检测管理员离线/`MailAdminEditPayload(cancel=true)` 取消时恢复 `hidden=false`）。

---

## 14. 调试日志约定
- `SentMailRepository`：`put/remove/update` 的 entering/exiting/异常。
- `MailDataStorage`：`load/save/addRef/updateRef/removeRef` 读写锁与加载剔除计数。
- `MailManager.send`：权限结果、接收者展开数量、每封写入、在线推送；`claim`：附件逐项日志 + 背包满掉落；`recall`/`edit`：仓库变更与推送。
- `MailComposeScreen`：打开、勾选切换、[发布] 组装与发送、服务端回包结果。
- `ModNetworking` / `ClientNetworking` 各处理器：`entering` + 权限分支 `branch` + `exiting`。

---

## 15. 建议实现阶段
1. **P1 数据层**：`Mail* ` 模型 + `SentMailRepository`/`MailDataStorage`/`MailSettings` + 日志。
2. **P2 网络层**：16 个 Payload + `ModNetworking`/`ClientNetworking` 注册与处理器。
3. **P3 客户端命令**：`MailClientCommand`（`send_mail`/`sent`/`recall`/`purge`/`list`，均解析后发 C2S 包）+ `MailPermissionHelper` + 非管理判定。
4. **P4 玩家信箱 GUI**：`MailScreen` + `MailList` + 详情 + 未读徽标（入口接 Shift+F 邮件按钮）+ [发布邮件]/[已发送邮件] 按钮。
5. **P5 管理员发布 / 编辑 GUI**：`MailClientCommand.send_mail` + `MailComposeOpenPayload`/`OpenMailComposePayload` + `MailComposeScreen`（多选范围/勾选/文本/10 物品槽选取/过期）+ `MailAdminSendPayload`（多 TargetSpec 并集）+ canSend 控制；**编辑模式**（`MailSentScreen`[编辑] → 预填 `MailComposeScreen` → `MailAdminEditPayload`）；`MailSentScreen` + `MailSentListPayload`；**正文多行编辑器**自研。 
6. **P6 过期与收藏**：加载清理 + 周期扫描 + 星标保留 + 批量操作。
7. **P7 本地化与联调**：全语言 key、devMode 全流程自测（compose→收件→领取→星标→过期→撤回→编辑→修改）。

---

## 16. 需求确认结论（已定）
| 项 | 结论 |
|---|---|
| 奖励类型 | 物品 / 指令(`%player%`/`%uuid%`) / 原版经验值 / 原版等级 / 本项目冒险等级 |
| 接收方 | 全体(含管理) / 全体非管理(由 `mail_permission_node` 或 OP 判定) / 指定玩家 / 角色组 |
| 过期 | 锁定 1天/7天/30天/永久，默认 30天 |
| 收藏过期 | 保留文本、禁用奖励领取 |
| 撤回/修改 | 撤回需 `recall` 命令；**修改走 GUI 编辑模式**（`MailSentScreen`[编辑] → 预填 `MailComposeScreen` → `MailAdminEditPayload`），仓库+索引模型支持 |
| 发布方式 | **GUI（`MailComposeScreen`）填写发送**；由命令 `/yzwc mail send_mail` 打开发布页（不再挂 event）；编辑亦走 GUI |
| 物品附件 | 支持从管理员**物品栏**选取，仅复制为模板不消耗；**最多 10 个物品槽位** |
| 接收范围 | **可多选，取并集去重**（全体 / 全体非管理 / 指定玩家[可多个] / 角色组[可多个]） |
| 离线覆盖 | **绑定账户系统**：`ALL`/`NONADMIN`/`ROLE` 以 `AccountDataStorage.getAll()` 全部已注册账户为全集解析，离线账户同样入索引、登录可见；账户注销时清空其收件箱（见 §7.3 / §10） |
| GUI 入口 | 玩家信箱经 Shift+F 邮件按钮打开；发布页经 `/yzwc mail send_mail` 命令或信箱内 [发布邮件] 按钮（管理员可见）；已发送管理列表经 `/yzwc mail sent` 或信箱内 [已发送邮件] 按钮 |
