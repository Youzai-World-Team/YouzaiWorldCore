# 2026-06-20 工作日志

## 守护之心数量提醒功能

为 `tick.mcfunction` 中的"守护之心"功能添加了数量提醒机制：

### 修改的文件
1. **load.mcfunction** — 新增 `youzaiworld.heart_count` 和 `youzaiworld.heart_warned` 记分板，初始化 `heart_warned=0`
2. **tick.mcfunction** — 
   - 第3行：旧 NBT 语法 `@a[nbt={Inventory:[{id:"..."}]}]` → `execute if items entity @s container.*`（26.2 新语法）
   - 追加警告逻辑：在死亡处理之后，使用 `execute if items` 非破坏性统计守护之心数量，在剩余 10/5/3/2/1 时通过翻译键发送警告
3. **zh_cn.json / en_us.json** — 各追加 5 个翻译键（空值，待用户填写）

### 技术要点
- 使用 `execute store result score ... if items entity @s container.*` 统计物品总数（26.2 原生方式）
- `heart_warned` 记分板 + `unless score =` 实现去重，每个阈值只警告一次
- `tellraw` 纯输出 `{"translate":"..."}`，不附加颜色修饰
